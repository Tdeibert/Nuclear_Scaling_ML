Here's the complete v8.1 development plan ready for the next session:

V8.1 Development Issues
Issue #1 — Reference mask anchoring (High Priority)
Current flow re-segments droplets at focus z, causing centroid drift between reference z and focus z. Patch centers derived from reference z land outside focus z droplets, producing very low droplet channel fractions (0.004–0.197 observed at t=3) despite min_label_fraction=0.002.
Fix: pass reference droplet mask directly into make_multilabel_target. Only NPC detection and nucleus segmentation run at focus z. Droplet geometry is anchored to reference segmentation throughout.
Change in _process_one_timepoint:
python# Current — re-segments droplets at focus z
target, metadata, debug = build_training_label_plane(
    npc2d=npc2d, nuc2d=nuc2d, droplet_kwargs=dk, ...)

# v8.1 — pass reference mask, only detect NPC + nucleus at focus z
target, metadata, debug = build_training_label_plane(
    npc2d=npc2d, nuc2d=nuc2d,
    droplet_mask_override=droplet_mask_ref, ...)
build_training_label_plane and make_multilabel_target need a droplet_mask_override parameter — if provided, skip segment_droplet_from_npc entirely and use the provided mask.
Issue #2 — Watershed separation for dense late timepoints (High Priority)
At t=6-9, densely packed droplets have no background between them. Adaptive threshold merges adjacent droplets into non-circular blobs that fail shape filters. Only 19-28 of ~100 real droplets detected.
Fix: distance transform + watershed separation added to segment_droplet_from_npc.
New config parameters:
pythondroplet_use_watershed: bool = False
droplet_watershed_min_distance: int = 20  # px between droplet centers
Per-timepoint override:
pythoncfg.per_timepoint_droplet_overrides = {
    6: {"use_watershed": True},
    7: {"use_watershed": True},
    8: {"use_watershed": True},
    9: {"use_watershed": True},
}
Implementation inside segment_droplet_from_npc after existing threshold + cleanup:
pythonif use_watershed:
    from skimage.segmentation import watershed
    from skimage.feature import peak_local_max
    distance = ndi.distance_transform_edt(droplet_mask)
    coords   = peak_local_max(distance, min_distance=droplet_watershed_min_distance,
                              labels=droplet_mask)
    markers  = np.zeros_like(droplet_mask, dtype=np.int32)
    markers[tuple(coords.T)] = np.arange(1, len(coords) + 1)
    droplet_mask = watershed(-distance, markers, mask=droplet_mask) > 0
Backward compatible — use_watershed=False default preserves exact v8 behavior for t=0-5.
Implementation order:

Issue #1 first — reference mask anchoring affects all timepoints and improves overall patch quality
Issue #2 second — watershed for late timepoints, calibrate min_distance via QC before running full generation

Expected impact:

t=3 with 100 droplets at patches_per_droplet=2 should recover from 23 → ~200 patches
t=6-9 should recover from 1 → ~100 droplets each
Total patches should increase from ~729 to well over 2000